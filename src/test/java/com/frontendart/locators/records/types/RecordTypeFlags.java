package com.frontendart.locators.records.types;

/**
 * Record flags (has required fields, has referred fields, has extendable fields)
 * @author Zoli
 *
 */
public enum RecordTypeFlags {
	HAS_REQUIRED_FIELDS,
	HAS_EXTENDABLE_FIELDS,
	NONE,
}
